# Python_Ansible_Gitlab_CICD
This Python code finds the shortest path to arrive at the destination.
use ansible to config the Ubuntu server.
use gitlabCI as ci_tool.
and deploy it in a docker container.
